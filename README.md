# OECD CRS DOCSTAT Parser

This tool parses OECD CRS dotStat `.txt` files and extracts records for a specified recipient country.
If `--recipient` is omitted, the tool converts the full CRS dataset.

## Requirements
- Python 3.9+
- pandas

## Installation
```bash
pip install -r requirements.txt
